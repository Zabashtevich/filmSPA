const initialState = { type: null, id: null, name: null, item: null };

const metalogicReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
