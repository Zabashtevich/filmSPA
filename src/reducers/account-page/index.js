const initialState = null;

export default function accountPageReducer(state = initialState, action) {
  switch (action.type) {
    case "hahah":
      return null;
    default:
      return state;
  }
}
